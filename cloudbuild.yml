steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - ${_IMAGE_URI}
      - .

images:
  - ${_IMAGE_URI}

substitutions:
  _IMAGE_URI: australia-southeast1-docker.pkg.dev/$PROJECT_ID/realtime-energy/importer:latest
